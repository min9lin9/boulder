Package dry-run evidence

Observed result on 2026-07-08.

Command:

```bash
bun pm pack --dry-run --ignore-scripts
```

Result:

```text
boulder-oss-cli-0.1.16.tgz
Total files: 168
Unpacked size: 0.63MB
```

Package exclusions verified AGENTS.md, localized session summaries, and localized gap plans are not packed.

Local candidate smoke:

```bash
bun pm pack --ignore-scripts --destination <temp-dir>
npm exec --cache <temp-dir>/npm-cache --package "file:<temp-dir>/boulder-oss-cli-0.1.16.tgz" -- boulder --version
```

Observed output:

```text
0.1.16
```
